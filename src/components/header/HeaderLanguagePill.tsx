'use client';

import { HeaderLanguageSwitcher } from './HeaderLanguageSwitcher';

/** Desktop navbar language control — re-exports shared segmented switcher. */
export function HeaderLanguagePill() {
  return <HeaderLanguageSwitcher />;
}
