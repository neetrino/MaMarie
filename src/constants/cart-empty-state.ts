/** Figma `66:477` — empty cart illustration width (px). */
export const CART_EMPTY_ILLUSTRATION_WIDTH_PX = 313;

/** Figma `66:477` — empty cart illustration height (px). */
export const CART_EMPTY_ILLUSTRATION_HEIGHT_PX = 302;

/** Empty cart title font size (px) — fits Armenian headline on one line in drawer. */
export const CART_EMPTY_TITLE_FONT_SIZE_PX = 20;

/** Figma `66:481`→`66:482` — gap between title and subtitle (px). */
export const CART_EMPTY_TITLE_TO_SUBTITLE_GAP_PX = 18;

/** Figma `66:482`→`66:483` — gap between subtitle and CTA (px). */
export const CART_EMPTY_SUBTITLE_TO_CTA_GAP_PX = 40;

/** Lift illustration + copy above the CTA without moving the button (px). */
export const CART_EMPTY_CONTENT_LIFT_PX = 10;

/** Figma `66:482` — empty cart subtitle font size (px). */
export const CART_EMPTY_SUBTITLE_FONT_SIZE_PX = 14;

/** Figma `66:482` — empty cart subtitle color. */
export const CART_EMPTY_SUBTITLE_COLOR = '#6f7384';

/** Figma `66:484` — empty cart CTA label font size (px). */
export const CART_EMPTY_CTA_FONT_SIZE_PX = 14;

/** Figma `66:483` — empty cart CTA height (px). */
export const CART_EMPTY_CTA_HEIGHT_PX = 50;

/** Figma `66:485` — empty cart CTA icon circle size (px). */
export const CART_EMPTY_CTA_ICON_SIZE_PX = 40;

/** Figma `66:486` — empty cart CTA arrow icon size (px). */
export const CART_EMPTY_CTA_ARROW_SIZE_PX = 20;

export const CART_EMPTY_ASSETS = {
  illustration: '/assets/cart/empty-basket.png',
  ctaArrow: '/assets/cart/empty-cta-arrow.svg',
} as const;
