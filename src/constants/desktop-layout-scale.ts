import { HOME_SECTION_MAX_WIDTH_PX } from './home-sections';

/** Figma desktop artboard — reference width for proportional layout scaling. */
export const DESKTOP_LAYOUT_DESIGN_WIDTH_PX = HOME_SECTION_MAX_WIDTH_PX;

/** Tailwind `lg` breakpoint — fluid scaling applies at and above this width. */
export const DESKTOP_LAYOUT_MIN_VIEWPORT_PX = 1024;
