/** Figma `66:506` — empty wishlist illustration width (px). */
export const WISHLIST_EMPTY_ILLUSTRATION_WIDTH_PX = 328;

/** Figma `66:506` — empty wishlist illustration height (px). */
export const WISHLIST_EMPTY_ILLUSTRATION_HEIGHT_PX = 299;

/** Figma `66:505`→`66:507` — gap between illustration and copy block (px). */
export const WISHLIST_EMPTY_ILLUSTRATION_TO_COPY_GAP_PX = 16;

/** Figma `66:510` — empty wishlist title font size (px). */
export const WISHLIST_EMPTY_TITLE_FONT_SIZE_PX = 24;

/** Figma `66:510` — empty wishlist title max width (px). */
export const WISHLIST_EMPTY_TITLE_MAX_WIDTH_PX = 284;

/** Figma `66:509` — gap between title and subtitle (px). */
export const WISHLIST_EMPTY_TITLE_TO_SUBTITLE_GAP_PX = 5;

/** Figma `66:508` — gap between copy block and CTA (px). */
export const WISHLIST_EMPTY_COPY_TO_CTA_GAP_PX = 35;

/** Figma `66:511` — empty wishlist subtitle font size (px). */
export const WISHLIST_EMPTY_SUBTITLE_FONT_SIZE_PX = 14;

/** Figma `66:511` — empty wishlist subtitle color. */
export const WISHLIST_EMPTY_SUBTITLE_COLOR = '#6f7384';

/** Figma `66:512` — empty wishlist content max width (px). */
export const WISHLIST_EMPTY_CONTENT_MAX_WIDTH_PX = 328;

/** Figma `66:513` — empty wishlist CTA label font size (px). */
export const WISHLIST_EMPTY_CTA_FONT_SIZE_PX = 14;

/** Figma `66:512` — empty wishlist CTA height (px). */
export const WISHLIST_EMPTY_CTA_HEIGHT_PX = 50;

/** Figma `66:514` — empty wishlist CTA icon circle size (px). */
export const WISHLIST_EMPTY_CTA_ICON_SIZE_PX = 40;

/** Figma `66:515` — empty wishlist CTA arrow icon size (px). */
export const WISHLIST_EMPTY_CTA_ARROW_SIZE_PX = 20;

/** Top padding above empty wishlist content block (px). */
export const WISHLIST_EMPTY_SECTION_PADDING_TOP_PX = 0;

/** Gap below page title when wishlist is empty (px). */
export const WISHLIST_EMPTY_TITLE_MARGIN_BOTTOM_PX = 16;

/** Wishlist page title — pink section style, slightly smaller than best products (px). */
export const WISHLIST_PAGE_TITLE_FONT_SIZE_PX = 40;
export const WISHLIST_PAGE_TITLE_LINE_HEIGHT_PX = 44;
export const WISHLIST_PAGE_HEADING_MIN_HEIGHT_PX = 52;

export const WISHLIST_EMPTY_ASSETS = {
  illustration: '/assets/wishlist/empty-heart.png',
  ctaArrow: '/assets/cart/empty-cta-arrow.svg',
} as const;
