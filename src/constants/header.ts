/** Scroll distance before home navbar switches to pill mode. */
export const HEADER_HOME_SCROLL_THRESHOLD_PX = 12;

/** Top inset when navbar overlays the hero. */
export const HEADER_HOME_OVERLAY_TOP_PX = 8;

/** Pill fade-in duration (ms). */
export const HEADER_PILL_APPEAR_DURATION_MS = 500;

/** Horizontal inset of pill background — content keeps Figma 87/93px inset. */
export const HEADER_PILL_SHELL_PADDING_X_PX = 16;
