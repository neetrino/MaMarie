import { NotFoundStandaloneLayout } from '../../components/not-found/NotFoundStandaloneLayout';

/** Admin 404 — no public navbar; white surface, centered clay scene. */
export default function SupersudoNotFound() {
  return <NotFoundStandaloneLayout />;
}
